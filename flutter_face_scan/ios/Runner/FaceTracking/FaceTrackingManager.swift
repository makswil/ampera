// Intentionally empty.
//
// The TrueDepth face-tracking implementation now lives in
// ios/Runner/AppDelegate.swift, which is already part of the Runner build
// target (no Xcode "Target Membership" step required).
//
// If you prefer the original split layout, move FaceTrackingManager /
// FacePreviewFactory / FaceTrackingPlugin back into these files AND add them to
// the Runner target (Build Phases → Compile Sources), then remove them from
// AppDelegate.swift to avoid duplicate symbols.
